[Unit]
Description=Compact football captures daily

[Timer]
# 10:30 UTC, after the football collector at 10:00. The collector gzips what
# it consumed, so this only sweeps up captures it could not settle.
OnCalendar=*-*-* 10:30:00
Persistent=true

[Install]
WantedBy=timers.target
